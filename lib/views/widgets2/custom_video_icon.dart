import 'package:flutter/material.dart';
class CustomVideoIcon extends StatelessWidget {
  const CustomVideoIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(

    Icons.slow_motion_video,

    size: 90,
    color: Colors.grey[400],
    );

  }
}
