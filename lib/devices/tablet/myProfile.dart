import 'package:flutter/material.dart';
class TabletMyProfile extends StatelessWidget {
  const TabletMyProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}
